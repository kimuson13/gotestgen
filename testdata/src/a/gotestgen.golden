package a_test

func Testhoge(t *tesitng.T) {
	cases := map[string]struct {
		// write arguments below this

	}{
		// write test cases below this
		// test case name: {args}

	}

	for testName, tt := range cases {
		tt := tt
		t.Run(testName, func(t *testing.T) {
			// write tests below this

		})
	}
}
